name: kaizhou

services:
  db:
    image: postgres:16-alpine
    container_name: kaizhou-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: kaizhou
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in .env}
      POSTGRES_DB: kaizhou
    volumes:
      - db_data:/var/lib/postgresql/data
    # No healthcheck: polling pg_isready every 10s kept the NAS disks awake (audible
    # noise). The app waits on service_started instead (fine in practice on the NAS).
    # No host port published — only reachable by the app over the internal network.

  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL:?set NEXT_PUBLIC_SITE_URL in .env}
    image: kaizhou-app:latest
    container_name: kaizhou-app
    restart: unless-stopped
    depends_on:
      db:
        condition: service_started
    environment:
      NODE_ENV: production
      PORT: "3000"
      DATABASE_URL: postgres://kaizhou:${POSTGRES_PASSWORD}@db:5432/kaizhou
      PAYLOAD_SECRET: ${PAYLOAD_SECRET:?set PAYLOAD_SECRET in .env}
      NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL}
      # Microsoft Entra ID (Azure AD) SSO. REQUIRED in prod: prod runs with
      # disableLocalStrategy (no email/password), so if these are unset the plugin
      # stays off and the admin panel has NO way to log in. Fail-fast on missing.
      ENTRA_TENANT_ID: ${ENTRA_TENANT_ID:?set ENTRA_TENANT_ID in .env}
      ENTRA_CLIENT_ID: ${ENTRA_CLIENT_ID:?set ENTRA_CLIENT_ID in .env}
      ENTRA_CLIENT_SECRET: ${ENTRA_CLIENT_SECRET:?set ENTRA_CLIENT_SECRET in .env}
    volumes:
      - media:/app/media
    ports:
      # Host 3100 -> container 3000. Cloudflare Tunnel routes kaizhou.hk here.
      - "3100:3000"

volumes:
  db_data:
  media:
